# =============================================================================
# requirements-docker.txt
# =============================================================================
# Used ONLY by the Dockerfile.
# torch is intentionally excluded — the Dockerfile installs CPU-only torch
# separately via --index-url https://download.pytorch.org/whl/cpu before
# this file, avoiding the default 2 GB CUDA wheel.
# =============================================================================

requests>=2.31.0
numpy>=1.26.0
pandas>=2.1.0
sentence-transformers>=2.7.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
scipy>=1.12.0

chromadb>=0.5.0
networkx>=3.3
nltk>=3.8.0

datasets>=2.19.0
biopython>=1.83
requests-cache>=1.2.0

pyvis>=0.3.2
transformers>=4.40.0

fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
