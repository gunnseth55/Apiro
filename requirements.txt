# Calibration experiment deps (already installed)
requests>=2.31.0
numpy>=1.26.0
pandas>=2.1.0
sentence-transformers>=2.7.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
scipy>=1.12.0

# Phase 1 — Corpus & Infrastructure
chromadb>=0.5.0
networkx>=3.3
nltk>=3.8.0

# Corpus sources
datasets>=2.19.0            # HuggingFace datasets (MedRAG/pubmed streaming)
biopython>=1.83             # ClinVar FTP gz parsing (gzip stdlib used instead)
requests-cache>=1.2.0       # Optional: cache HTTP requests during development

# Phase 2 — Graph Engine
pyvis>=0.3.2

# Phase 5 - HADCE NLP Pipeline
pyyaml>=6.0.1
transformers>=4.40.0
torch>=2.2.0

# Phase 4 — API & Interface
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
